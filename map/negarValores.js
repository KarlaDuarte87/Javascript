/*Dado um array de números, 
use map para criar um novo array
 onde cada número é negado (multiplicado por -1). */

 const numeros = [1,2,3,4,5];
 const negado = numeros.map( x => x *-1);
console.log(negado);
